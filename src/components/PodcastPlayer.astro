---
interface Props {
  audioUrl: string;
}

const { audioUrl } = Astro.props;
---

<div
  class='w-full flex flex-col items-center justify-center py-4 border-[1.5px] border-slate-100 mt-2 p-6 sm:p-9 sm:px-10 bg-white rounded-md'
>
  <audio src={audioUrl} controls class='w-full bg-white' preload='metadata'></audio>
  <span class='text-slate-500 text-sm mt-2 flex items-center justify-center text-center'>
    この音声は AI によって生成されており、誤りが含まれる可能性があります
  </span>
</div>
